Immediate attention:
The database configuration tab is gone.
Where is the account type radio buttons?
Search in User Management is not doing anything meaningful.
Fix the data management window actions section options such as export report card, create backup card, restore from backup card are extending to so much amount of height of the display. Remove the excess vertical spacing, it is not needed.
When I see the email in mobile, the svg logo is absent and on computer there is no space between the logo and library os. 
The invoice generation should not be static on circulation view, because the partial amount can be paid. How we generate complete invoice then? Also in invoice generation dialog, send email and print receipt buttons text are not completely visible, filled with partial text. Also they are not styled. Why there is a question mark dialog, it is a success, right?
If I switch the theme inside settings section or inside any options, the themes are not applying properly.
I said if it is card then 3 at most, each card should occupy the 1/3 of the viewport width regardless of the size and must be responsive. Also when the theme changed or app is refreshed, now the cards in dashboard are large after auto or manual refresh or after I hover the mouse over it, it shrinks but it doesn't come back to large size, the mouse hover on card should not change anything so fix it properly (Very Urgent).
In the issue book dialog, I can now select more than one book randomly. I can see a small rectangle even if no book is selected. When I search for another book, the selected book is deleted. You should make the textbox size of issue date smaller and date picker texts days color contrast must be visible in dark theme. Also in light theme, there are excess borders around the calendar icon and there is a background color (gray), remove it.
There is no back button <- like this, to go to previous screen on the left corner.
Library Field is now working but initially the list is too far away from the box almost present at the username textbox when we type something it comes somewhat near. Fix it properly. The scroll bar is not applied proper color in dark theme since it is white. Also inject 10 to 15 library, so that we can test the UI.
Implement tooltip properly for every icon, if the mouse is placed over, it should stay displaying but now it is shown and then hide even if the mouse is there. Also there is no subtle disappearing creating disturbance.
While I set light theme, in the register page account type contains radio buttons but only green circle is visible not looking like a radio. In dark theme, the small white dot is visible but not centered as shown in screenshot.
When I change the theme while on circulation view, the first tab is automatically selected.

Improvements:
I can see the font size differs between the themes.
The icon is not visible in the taskbar when the run icon from IDE gutter is hit. Also I don't know about the installers icon and taskbar icon (app-icon.svg).
You now bring the db tab, but when the existing data is copied to db also the files are read while the app starts, similarly if the files are not found, the db should be used for fetching.
Where are the library name and branch are stored in file as well as db?

Enhancements:
Most of the buttons ok, cancel, no, close is looking plain, not styled, colored, etc., across the app.
Also, there is no transitions and animations I can find, I don't say there is nothing but is too minimal.
The latest version of all dependencies must be ensured.
Code needs to be organized in a better way by refactoring code, migrating packages from old to new, etc.,