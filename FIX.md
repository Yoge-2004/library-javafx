Add the svg icon (our logo) of our app to the email.
How two users can have same email, mobile number as well as username, you must change it. Because when I edit the email, phone, username of a user from the user management dialog, the email, phone, username duplication is not validated.
(java:10108): Gdk-WARNING **: 11:31:56.003: XSetErrorHandler() called with a GDK error trap pushed. Don't do that.

Where are the library name and branch are stored, so it is used in datalist (library field) while logging in.
When the internet is disconnected and tried to send email, showing an error message cannot connect to the host but you can say there is no network connectivity if the internet (wired/wireless) not connected.
Library Field Issue is now working but inject 10 to 15 library, we can test it, that is scrollable list, because if I have 100 libraries, then it is not possible to show, show 3 libs at a time remaining should be scrollable.
I said if it is card then 3 at most, each chart should occupy the viewport width regardless of the size and must be responsive.
The bottom part (footer) section is not visible due to the window size like last refresh, also no windows and dialogs can respond to -, square of the OS (located in the top right corner)
When the theme changed, the cards are large after auto or manual refresh it shrink, fix it properly.
Implement tooltip properly for every icon, if the mouse is placed over, it should stay displaying but now it is flashing very fast after I replaced the modified code, it should not flash, but to stay until the mouse does not hover. 
Every button is looking plain, not styled, colored, etc.,
This app is going to be packed as Windows exe, Linux deb, Mac dmg, so verify the implemented logging and store it safely based on the OS without any additional permissions.
In dashboard page, the text "Library OS", "Navigation", "Management", "Account" and the bottom left where the username and role (E.g, Admin Administrator) in light theme, the color contrast should be changed to more white or any color which is contrast.
Also, there is no transitions and animations I can find, I don't say there is nothing but is too minimal.
The print feature is implemented but without opening the print dialog, the app is showing no printers found toast message, the user can store it as pdf too, I asked you to open the system print dialog.
The icon is not visible in the taskbar when the run icon from IDE gutter is hit. Also I don't know about the installers.
You now bring the db tab, but when the existing data is copied to db also the files are read while the app starts, similarly if the files are not found, the db should be used for fetching, where is the oracle sql?
Pie chart color is not changing after refresh only the pie chart legends color changing.
The toast should be horizontal but now its vertical but the center position is correct and small as much as possible but display the whole message. Don't you even know how toast looks?
Code needs to be organized in a better way by refactoring code, migrating packages from old to new, etc.,