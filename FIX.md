Critical Improvements:
Radio button in registration can be improved because the circle is inside the square, the square should be removed.
Also, did we set the email configurations globally so that everyone can use?
Who will create the library name and branch but you are asking it as a dropdown so is it stored as a file to list all the registered libraries?
Can you improve and enhance the UI/UX? The layout is good, just change the color schemes and navigation.
You focused dark theme only on the home page not other pages properly.
The +, - icon is missing in the library configuration borrowing tab. Also the text box looks weird.
Also the restore and minimise button is working for the main window not for others, also if I click any option in settings accidentally. I need to close that option, I need to reopen the settings from the main window. It affects the UX, better we should improve the navigation.
Sign in spinner is not center aligned.
When I delete a character in the login window (library) field, it goes to next text box (username).
The app icon is not visible on the taskbar.
The error message is not completely shown after I click forgot password and give the name.
The circulation trends chart and other charts below it and Recent issues and Top Overdue can utilize the remaining horizontal space.
In overdue section, there is no filter and sorting of records.



