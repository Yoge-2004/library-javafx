First see the screenshots in the project root directory.
Where are the library name and branch are stored, so it is used in datalist (library field) while logging in.
When the internet is disconnected and tried to send email, showing an error message cannot connect to the host but you can say there is no network connectivity if the internet (wired/wireless) not connected.
Library Field Issue is now working but inject 10 to 15 library, we can test it, that should be scrollable list, because if I have 100 libraries, then it is not possible to show, show 3 libs at a time remaining should be scrollable.
I said if it is card then 3 at most, each chart should occupy the viewport width regardless of the size and must be responsive.
When the theme changed or app is refreshed, the cards in dashboard are large after auto or manual refresh it shrink when I hover over it, fix it properly.
In dashboard page, the text "Library OS", "Navigation", "Management", "Account" and the bottom left where the username and role (E.g, Admin Administrator) in light theme, the color contrast should be changed to more white or any color which is contrast.
The print feature is implemented but without opening the print dialog, the app is showing could not start system print dialog toast message, the user can store it as pdf too.
The icon is not visible in the taskbar when the run icon from IDE gutter is hit. Also I don't know about the installers icon and taskbar icon (app-icon.svg).
You now bring the db tab, but when the existing data is copied to db also the files are read while the app starts, similarly if the files are not found, the db should be used for fetching.
Also in add new book, around the category text field there is a extra border (visible in dark theme).
Implement tooltip properly for every icon, if the mouse is placed over, it should stay displaying but now it is flashing, but to stay until the mouse focus on the element is lost. Now it is flashing.
See the edit profile and password photos, the left side text (prompt) for the fields are gray which is not visible properly in dark theme. You given rounded rectangle for each and every option in the home page.
Also, there is no transitions and animations I can find, I don't say there is nothing but is too minimal.
Code needs to be organized in a better way by refactoring code, migrating packages from old to new, etc.,
This app is going to be packed as Windows exe, Linux deb, Mac dmg, so verify the implemented logging and store it safely based on the OS without any additional permissions.
Every button is looking plain, not styled, colored, etc.,
Fix the data management window actions section as shown in the figure the text is overflowing and not visible properly.
The toast should be horizontally occupied but now its vertical but the center position is correct and it should be small as much as possible but display the whole message. Don't you even know how toast looks?
No windows and dialogs can respond to -, square of the OS (located in the top right corner). Always adapt the window size based on the viewport width and height.
Add the svg icon (our logo [app-icon.svg]) of our app to the email.
